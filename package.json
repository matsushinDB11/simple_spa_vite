{
  "name": "simple_spa_vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prebuild": "node -e 'fs.rmSync(`dist`, {recursive:true, force:true})'",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sass": "^1.72.0",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.3"
  }
}
